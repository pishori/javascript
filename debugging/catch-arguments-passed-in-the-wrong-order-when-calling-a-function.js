function raiseToPower(e, b) {   // Should raise the base to an exponent
  return Math.pow(e, b);
}

let base = 2;
let exp = 3;
let power = raiseToPower(base, exp);
console.log(power); // returns 9

/*function raiseToPower(b, e) {
  return Math.pow(b, e);
}

let base = 2;
let exp = 3;
let power = raiseToPower(exp, base);
console.log(power); // returns 9*/

